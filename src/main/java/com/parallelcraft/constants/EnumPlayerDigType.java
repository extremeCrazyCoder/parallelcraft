package com.parallelcraft.constants;

/**
 * Basically a copy of what MC uses, needs to be same since this is part of the protocol
 * 
 * @author extremeCrazyCoder
 */
public enum EnumPlayerDigType {
    START_DESTROY_BLOCK,
    ABORT_DESTROY_BLOCK,
    STOP_DESTROY_BLOCK,
    DROP_ALL_ITEMS,
    DROP_ITEM,
    RELEASE_USE_ITEM,
    SWAP_ITEM_WITH_OFFHAND
}
